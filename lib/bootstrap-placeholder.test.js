/**
 * Placeholder so `npm test` exits 0 from day one of the standup.
 * Replaced by the real `bootstrap.test.js` once `bootstrap()` lands.
 */

describe('chassis', () => {
  test('placeholder until the real shell files arrive', () => {
    expect(true).toBe(true);
  });
});
